[project]
name = "tools"
version = "0.0.1"
description = "Dataset parquet -> arrow conversion tool"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pillow>=12.1.1",
    "polars>=1.38.1",
    "pyarrow>=23.0.1",
]
